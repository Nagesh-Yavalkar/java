class Demo{
    int z = 10;
    public static void main(String[] args){
         Demo obj = new Demo();    // whenever you create object then call to constructor for intialize the instance variable with help of this
                                    // whenever you create an object two works happen that is 
                                    // first is memory assign on heap for instance variable
                                    // and second in call the constructor
    }
}